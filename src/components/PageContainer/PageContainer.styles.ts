import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledPageContainerBox = styled(Box)({
  boxSizing: 'border-box',
  flex: '1 1 auto',
  padding: '2rem 0',
  margin: '0 auto',
  maxWidth: '700px',
  display: 'flex',
  flexDirection: 'column',
  height: '100vh',
});
